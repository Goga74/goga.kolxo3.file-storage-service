package goga.kolxo3.filestorage.controller;

//import goga.kolxo3.filestorage.database.DatabaseService;
//import io.vertx.core.AbstractVerticle;
//import io.vertx.core.Promise;

/*
public class DatabaseController extends AbstractVerticle {
    private DatabaseService databaseService;

    @Override
    public void start(Promise<Void> startPromise) {
        String dbUrl = "jdbc:mysql://localhost:3306/your_database_name"; // Укажите имя вашей базы данных
        String dbUser = "root"; // Ваш пользователь
        String dbPassword = "your_password"; // Ваш пароль

        databaseService = new DatabaseService(dbUrl, dbUser, dbPassword);

        // Пример выполнения запроса
        databaseService.executeQuery("SELECT * FROM your_table_name");

        startPromise.complete();
    }
}
*/